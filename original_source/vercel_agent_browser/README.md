# vercel_agent_browser

Preserved source module for agent-browser CLI usage contracts.

- Stores the stable command interaction model used by NanoClaw (`open`, `snapshot`, `click`, `fill`, `get`, `wait`).
- Used as provenance for catalog browser toolsets and runtime browser-stage policy.
