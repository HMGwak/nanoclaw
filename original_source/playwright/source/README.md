# Source Snapshot Notes

NanoClaw references `playwright-core` runtime contracts inside the container agent runner.

This preserved module stores fallback-stage behavioral contracts, not a full upstream mirror.
