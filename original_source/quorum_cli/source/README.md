# quorum_cli source notes

This directory is reserved for upstream raw snippets or provenance artifacts
when they need to be stored locally.

Current module keeps only normalized preserved docs under `../docs/`.
