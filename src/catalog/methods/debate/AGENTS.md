# Debate Methods

This folder defines reusable debate/consensus methodology for Nanoclaw.

Rules:

- Keep mode semantics and protocol contracts independent from service deployment.
- Put only reusable method definitions here.
- Service-specific participant mapping and role assignment must stay in `src/services/*`.
